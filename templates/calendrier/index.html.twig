<<<<<<< HEAD
{% extends 'base.html.twig' %}
{% block javascripts %}
    <script src="//cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.6.0/js/bootstrap-datepicker.min.js"></script>
    <script>
       $(document).ready(function() {
        // you may need to change this code if you are not using Bootstrap Datepicker
        $('.js-datepicker').datepicker({
            format: 'yyyy-mm-dd'
        });
    });
    </script>
{% endblock %}
=======
{% extends 'back.html.twig' %}
>>>>>>> c4fe70cb8140f168e5479a2d01008e006d67f03f

{% block title %}Calendrier index{% endblock %}

{% block body %}
    <h1>Calendrier index</h1>
    <div class="col-lg-12">
        <div class="table-responsive table--no-card m-b-40">
            <table class="table table-borderless table-striped table-earning">
                <thead>
                <tr>
                    <th>Id</th>
                    <th>Start_date</th>
                    <th>End_date</th>
                    <th>Id_coach</th>
                    <th>Id_sportif</th>
                    <th>Titre</th>
                    <th>Created_at</th>
                    <th>Modified_at</th>
                    <th>actions</th>
                </tr>
                </thead>
                <tbody>
                {% for calendrier in calendriers %}

<<<<<<< HEAD
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Start_date</th>
                <th>End_date</th>
                <th>Id_coach</th>
                <th>Id_sportif</th>
                <th>Titre</th>
                <th>Created_at</th>
                <th>Modified_at</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for calendrier in calendriers %}
            <tr>
                <td>{{ calendrier.id }}</td> 
                <td>{{ calendrier.startDate ? calendrier.startDate|date('d-m-Y H:i:s') : '' }}</td>
                <td>{{ calendrier.endDate ? calendrier.endDate|date('d-m-Y H:i:s') : '' }}</td>
                
                
                
                <td>
                {% if calendrier.coach is not empty %}
                    {{ calendrier.coach.nom }}
                {% endif %}
                </td>
                 <td>{% if calendrier.sportif is not empty %}
                {{ calendrier.sportif.nom }}
                 {% endif %}
              </td>
                <td>{{ calendrier.titre }}</td>
                <td>{{ calendrier.createdAt ? calendrier.createdAt|date('d-m-Y H:i:s') : '' }}</td>
                <td>{{ calendrier.modifiedAt ? calendrier.modifiedAt|date('d-m-Y H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('calendrier_show', {'id': calendrier.id}) }}">show</a>
                    <a href="{{ path('calendrier_edit', {'id': calendrier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
=======
                    <tr>
                        <td><a href="{{ path('calendrier_show', {'id': calendrier.id}) }}">{{ calendrier.id }}</a></td>
                        <td>{{ calendrier.startDate ? calendrier.startDate|date('Y-m-d H:i:s') : '' }}</td>
                        <td>{{ calendrier.endDate ? calendrier.endDate|date('Y-m-d H:i:s') : '' }}</td>
                        <td>{{ calendrier.idCoach }}</td>
                        <td>{{ calendrier.idSportif }}</td>
                        <td>{{ calendrier.titre }}</td>
                        <td>{{ calendrier.createdAt ? calendrier.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                        <td>{{ calendrier.modifiedAt ? calendrier.modifiedAt|date('Y-m-d H:i:s') : '' }}</td>
                        <td>
                            <a href="{{ path('calendrier_edit', {'id': calendrier.id}) }}">Editer</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="18">Aucune donnée disponible.</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
>>>>>>> c4fe70cb8140f168e5479a2d01008e006d67f03f


    <a href="{{ path('calendrier_new') }}">Créer un RDV</a>
{% endblock %}
