{% extends '@AdminBundle/base.html.twig' %}

{% block title %}Calendrier{% endblock %}

{% block stylesheets %}{{ parent() }}{% endblock %}

{% block page_title %}Gestion des utilisateurs{% endblock %}

{% block content %}
    <h1>Calendrier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calendrier.id }}</td>
            </tr>
            <tr>
                <th>Start_date</th>
                <td>{{ calendrier.startDate ? calendrier.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End_date</th>
                <td>{{ calendrier.endDate ? calendrier.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Id_coach</th>
                <td>{{ calendrier.coach.nom }}</td>
            </tr>
            <tr>
                <th>Id_sportif</th>
                <td>{{ calendrier.sportif.nom }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ calendrier.titre }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ calendrier.createdAt ? calendrier.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Modified_at</th>
                <td>{{ calendrier.modifiedAt ? calendrier.modifiedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('calendrier_index') }}">Retour à la liste</a>

    <a href="{{ path('calendrier_edit', {'id': calendrier.id}) }}">Editer</a>

    {{ include('calendrier/_delete_form.html.twig') }}
{% endblock %}

{% block javascripts %}{{ parent() }}{% endblock %}
