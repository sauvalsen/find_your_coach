{% extends '@AdminBundle/base.html.twig' %}

{% block title %}Calendrier{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block page_title %}<h1 class="title-calendrier">Calendrier</h1>{% endblock %}

{% block titre_menu %}<li>Calendrier</li>{% endblock %}

{% block nom_de_page %}Calendrier{% endblock %}

{% block content %}
    <h2 class="title-content">Calendrier de {{ calendrier.coach.prenom }} {{ calendrier.coach.nom }}</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calendrier.id }}</td>

                <th></th>
                <td></td>
            </tr>
            <tr>
                <th>Start_date</th>
                <td>{{ calendrier.startDate ? calendrier.startDate|date('Y-m-d H:i:s') : '' }}</td>

                <th>End_date</th>
                <td>{{ calendrier.endDate ? calendrier.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Id_coach</th>
                <td>{{ calendrier.coach.prenom }} {{ calendrier.coach.nom }}</td>

                <th>Id_sportif</th>
                <td>{{ calendrier.sportif.prenom }} {{ calendrier.sportif.nom }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ calendrier.titre }}</td>

                <th></th>
                <td></td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ calendrier.createdAt ? calendrier.createdAt|date('Y-m-d H:i:s') : '' }}</td>

                <th>Modified_at</th>
                <td>{{ calendrier.modifiedAt ? calendrier.modifiedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="bouton-perso" href="{{ path('admin_calendrier_index') }}">Retour a la liste</a>

    <a class="bouton-perso" href="{{ path('admin_calendrier_edit', {'id': calendrier.id}) }}">Éditer</a>

    {{ include('admin/calendrier/_delete_form.html.twig') }}
{% endblock %}

{% block javascripts %}{{ parent() }}{% endblock %}
